<%= form_for(@item, url: project_items_path) do |form| %>
  <% if @item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item.errors.count, "error") %> prohibited this item from being saved:</h2>
      <ul>
      <% @item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
      <div class="field">
      <%= form.label :content %>
      <%= form.text_field :content %>
      </div>
      <br>
     <div class="field">
      <%= form.label :make %>
      <%= form.text_field :make %>
      </div>
      <br>
   <div class="field">
      <%= form.label :model %>
      <%= form.text_field :model %>
    </div>
      <br>

    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
